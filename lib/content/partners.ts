export interface Partner {
  id: string;
  name: string;
  logo: string;
}

export const partners: Partner[] = [
  // Screenshot shows the Novellah logo itself repeated — likely placeholder.
  // Real partner/affiliate logos needed here once available.
];